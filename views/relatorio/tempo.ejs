<!DOCTYPE html>
<html>
    <head>
        <title>MinhaEmpresa</title>
        <link rel="stylesheet" href="/stylesheets/style.css" />
    </head>
    <body>
        <h1>Relatório de tempo de serviço</h1>
        <h3>Bem-vindo(a) ao MinhaEmpresa</h3>
        <table>
            <tr>
                <h3>Funcionários:</h3>
            </tr>
            <tr>
                <th>Nome</th>
                <th>Tempo na empresa (em dias)</th>
            </tr>
            <% for (func of data){ %>
            <tr>
                <td>
                    <a href="<%='/' +func.id %>"> <%= func.nome %> </a>
                </td>
                <td><%= func.tempo_servico %></td>
            </tr>
            <% } %>
        </table>
        <a href="/"><button>Dashboard</button></a>
    </body>
</html>
